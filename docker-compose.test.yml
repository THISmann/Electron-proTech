# Override pour l'environnement TEST (déploiement depuis la branche develop).
# Le site est exposé sur le port 8080 pour ne pas conflit avec la production (80).

services:
  traefik:
    ports:
      - "8080:80"
      - "8081:8080"
